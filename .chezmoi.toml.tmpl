{{- $email := promptStringOnce . "email" "Enter your Git email address" "peloeil20@gmail.com" -}}
{{- $name := promptStringOnce . "name" "Enter your Git full name" "Shun Ota" -}}
{{- $machine_type := promptStringOnce . "name" "Enter machine type (desktop or server)" "desktop" -}}
[data]
  email = "{{ $email }}"
  name = "{{ $name }}"
  machine_type = "{{ $machine_type }}"
  is_desktop = {{ contains "desktop" $machine_type }}
  is_server = {{ contains "server" $machine_type }}

[edit]
  command = "nvim"
